# Адаптивна система UI для Volunteer+

## Огляд

Платформа Volunteer+ тепер має інтелектуальну систему адаптації UI, яка автоматично допомагає користувачам з різними рівнями цифрової грамотності, особливо старшому поколінню.

## Реалізовані функції

### 1. Виправлений навбар
- **Проблема**: Пункти меню "Для волонтерів" та "Для організацій" переносилися на другий рядок
- **Рішення**: 
  - Зменшено відступи між елементами (`gap-1.5` замість `gap-2`)
  - Зменшено padding кнопок (`px-2` замість `px-4`)
  - Зменшено розмір шрифту (`text-sm`)
  - Додано `whitespace-nowrap` щоб текст не переносився

### 2. Відстеження промахів по кнопках
Система автоматично відстежує:
- Кліки біля кнопок (в радіусі 30px)
- Успішність натискання на інтерактивні елементи
- Підраховує кількість промахів підряд

**Логіка**:
```typescript
// Алгоритм виявлення промахів:
1. При кліку перевіряємо чи клікнули на кнопку/посилання
2. Якщо ні - шукаємо найближчу кнопку в радіусі 30px
3. Якщо знайдено - це промах, збільшуємо лічильник
4. При 3+ промахах підряд - збільшуємо UI на 15%
```

### 3. Автоматичне збільшення UI
- **Тригер**: 3 промахи по кнопках підряд
- **Збільшення**: +15% кожного разу
- **Максимум**: 175% від початкового розміру
- **Застосовується до**:
  - Розмір шрифтів (h1-h6, p, button, input, label, span, a)
  - Розмір кнопок (padding, min-height, min-width)
  - Розмір полів вводу

**Приклад масштабування**:
```
Початковий розмір: 100%
Після 3 промахів: 115%
Після 6 промахів: 130%
Після 9 промахів: 145%
Після 12 промахів: 160%
Після 15 промахів: 175% (максимум)
```

### 4. CSS система масштабування

Реалізовано дві системи:

#### `.scaled-ui` - Адаптивне масштабування
```css
--ui-scale: 1.0; /* Динамічна змінна, змінюється на основі поведінки */

.scaled-ui button {
  min-height: calc(44px * var(--ui-scale));
  padding: calc(0.5rem * var(--ui-scale));
}
```

#### `.large-ui` - Режим доступності
```css
--scale-factor: 1.3; /* Фіксоване збільшення на 30% */

.large-ui button {
  min-height: calc(44px * var(--scale-factor));
  padding: calc(0.75rem * var(--scale-factor));
}
```

#### Комбінація обох режимів
Якщо користувач увімкнув режим доступності І система виявила проблеми з натисканням:
```css
.large-ui.scaled-ui button {
  /* Застосовуються обидва множники */
  min-height: calc(44px * var(--scale-factor) * var(--ui-scale));
}
```

### 5. Користувацькі нотіфікації
При автоматичному збільшенні UI користувач отримує toast-повідомлення:
```
🔍 Інтерфейс збільшено до 115%
Ми помітили труднощі з натисканням кнопок 
і автоматично збільшили елементи
```

## Як це працює для користувача

### Сценарій 1: Молодий користувач
- Легко натискає на кнопки → UI залишається стандартним
- Швидка навігація, компактний інтерфейс

### Сценарій 2: Користувач старшого покоління
1. Намагається натиснути кнопку, але промахується
2. Клікає поруч з кнопкою (в радіусі 30px)
3. Система реєструє промах
4. Після 3 промахів:
   - UI автоматично збільшується на 15%
   - Показується повідомлення про збільшення
   - Кнопки стають більшими і легшими для натискання
5. Якщо проблеми продовжуються - UI збільшується ще більше

### Сценарій 3: Користувач з проблемами зору
1. Вручну вмикає "Режим доступності" → +30% до розміру
2. Якщо все одно є проблеми з натисканням → додаткове адаптивне збільшення
3. Максимальний розмір може досягти 175% × 130% = 227.5%

## Технічні деталі

### Файли, що змінились:
1. `/hooks/useUserBehavior.ts` - Логіка відстеження поведінки
2. `/App.tsx` - Застосування масштабування до DOM
3. `/components/Navigation.tsx` - Оптимізація навбара
4. `/styles/globals.css` - CSS правила масштабування
5. `/lib/router.tsx` - Покращена навігація

### Ключові функції:

#### `findNearestClickable(x, y, radius)`
Знаходить найближчу кнопку/посилання в радіусі від точки кліку

#### `isClickableElement(element)`
Перевіряє чи елемент є інтерактивним (button, a, [role="button"])

### Змінні стану:
```typescript
{
  uiScaleFactor: number;     // 1.0 - 1.75
  missedClicks: number;      // Кількість промахів
  clickDifficulty: boolean;  // Чи виявлено проблеми
  consecutiveMisses: number; // Промахи підряд
}
```

## Переваги системи

✅ **Автоматична адаптація** - не потребує ручного налаштування
✅ **Неінвазивна** - не заважає досвідченим користувачам
✅ **Прогресивна** - поступово збільшує UI по мірі потреби
✅ **Інформативна** - повідомляє користувача про зміни
✅ **Гнучка** - працює з режимом доступності або окремо
✅ **Інклюзивна** - допомагає користувачам різних вікових груп

## Майбутні покращення

- 🔄 Збереження налаштувань в localStorage
- 📊 Аналітика для покращення алгоритмів
- 🎯 Додаткові тригери (швидкість руху миші, час наведення)
- 🔧 Ручне керування рівнем масштабування
- 📱 Оптимізація для сенсорних екранів
